<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.activity.query.mapper.ContactQueryMapper">

    <resultMap id="contactResultMap" type="Contact">
        <id property="contactId" column="contact_id"/>
        <result property="clientId" column="client_id"/>
        <result property="writerId" column="writer_id"/>
        <result property="contactName" column="contact_name"/>
        <result property="contactPosition" column="contact_position"/>
        <result property="contactEmail" column="contact_email"/>
        <result property="contactTel" column="contact_tel"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="contactFilterConditions">
        <if test="clientId != null">
            AND client_id = #{clientId}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (contact_name LIKE CONCAT('%', #{keyword}, '%')
                 OR contact_email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </sql>

    <select id="findContactById" resultMap="contactResultMap">
        SELECT contact_id, client_id, writer_id, contact_name, contact_position,
               contact_email, contact_tel, created_at, updated_at
        FROM contacts
        WHERE contact_id = #{contactId}
    </select>

    <select id="findAllContacts" resultMap="contactResultMap">
        SELECT contact_id, client_id, writer_id, contact_name, contact_position,
               contact_email, contact_tel, created_at, updated_at
        FROM contacts
        ORDER BY created_at DESC
    </select>

    <select id="findAllContactsByClientId" resultMap="contactResultMap">
        SELECT contact_id, client_id, writer_id, contact_name, contact_position,
               contact_email, contact_tel, created_at, updated_at
        FROM contacts
        WHERE client_id = #{clientId}
        ORDER BY created_at DESC
    </select>

    <select id="findContactsWithFilters" resultMap="contactResultMap">
        SELECT contact_id, client_id, writer_id, contact_name, contact_position,
               contact_email, contact_tel, created_at, updated_at
        FROM contacts
        <where>
            <include refid="contactFilterConditions"/>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countContactsWithFilters" resultType="long">
        SELECT COUNT(*)
        FROM contacts
        <where>
            <include refid="contactFilterConditions"/>
        </where>
    </select>

</mapper>
